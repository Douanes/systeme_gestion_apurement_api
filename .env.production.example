# ===========================================
# VARIABLES D'ENVIRONNEMENT - SERVEUR DE PRODUCTION
# ===========================================
# Copiez ce fichier vers /opt/apurement/.env sur le serveur
# et remplacez les valeurs par vos vraies valeurs

# -------------------------------------------
# APPLICATION
# -------------------------------------------
NODE_ENV=production
PORT=3000

# -------------------------------------------
# BASE DE DONNÉES POSTGRESQL
# -------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
APUREMENT_DATABASE_URL=postgresql://username:password@localhost:5432/apurement_db?schema=public

# URL directe pour les migrations (même chose que DATABASE_URL)
APUREMENT_DIRECT_URL=postgresql://username:password@localhost:5432/apurement_db?schema=public

# -------------------------------------------
# SÉCURITÉ JWT
# -------------------------------------------
# Générer avec: openssl rand -base64 64
JWT_SECRET=votre_secret_jwt_tres_long_et_securise_ici

# Durée de validité du token en secondes (604800 = 7 jours)
JWT_EXPIRATION_SECONDS=604800

# -------------------------------------------
# BCRYPT (HASHING DES MOTS DE PASSE)
# -------------------------------------------
# Nombre de rounds pour bcrypt (10-12 recommandé, plus = plus sécurisé mais plus lent)
BCRYPT_ROUNDS=10

# -------------------------------------------
# SMTP (ENVOI D'EMAILS)
# -------------------------------------------
# Serveur SMTP
SMTP_HOST=smtp.gmail.com
# OU pour Outlook: smtp-mail.outlook.com
# OU pour un autre service: smtp.votre-service.com

# Port SMTP (587 pour TLS, 465 pour SSL, 25 pour non-sécurisé)
SMTP_PORT=587

# Utiliser TLS/SSL (true ou false)
SMTP_SECURE=false

# Identifiants SMTP
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre_mot_de_passe_application

# Adresse email d'expédition (affiché comme expéditeur)
SMTP_FROM=noreply@votre-domaine.com

# -------------------------------------------
# FRONTEND URL
# -------------------------------------------
# URL du frontend pour les liens dans les emails (activation, invitation, etc.)
FRONTEND_URL=https://votre-frontend.com

# -------------------------------------------
# AZURE BLOB STORAGE (STOCKAGE DE FICHIERS)
# -------------------------------------------
# Chaîne de connexion Azure Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=votre_compte;AccountKey=votre_cle;EndpointSuffix=core.windows.net

# Nom du conteneur pour les documents d'apurement
AZURE_APUREMENT_STORAGE_CONTAINER_NAME=apurement-documents

# -------------------------------------------
# DOCKER COMPOSE
# -------------------------------------------
# Version de l'image Docker à déployer
APUREMENT_VERSION=latest

# Cloudinary (Document Storage)
CLOUDINARY_CLOUD_NAME=your_actual_cloud_name
CLOUDINARY_API_KEY=your_actual_api_key
CLOUDINARY_API_SECRET=your_actual_api_secret
CLOUDINARY_UPLOAD_PRESET=apurement_documents
